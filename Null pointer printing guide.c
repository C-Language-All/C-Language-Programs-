#include<stdio.h>
int main()
{
	int a; //a is a variable
	int b=NULL;//b is null variable
	
	int *p; //p is a pointer
	int *q=NULL;//q is a null pointer	
printf("the value of a variable a is =\t%d\n",a);
printf("the value of null variable b is =\t%d\n",b);
printf("the value of pointer p is =\t%x\n",p);
printf("the value of null pointer q is =\t%x\n",q);
	
// you can also use %p instead of %x for printing pointer
	}
	
