#include<stdio.h>
int main()
{ int a=5,b=10,c=15;
	if(a>b&&a>c)
	{ printf("a is greater than b and c");
		}
		else if(b>a&&b>c)
		{ printf("b is greater than a and b");
			}
			else
			{ printf("c is greatest");
				}
				}
