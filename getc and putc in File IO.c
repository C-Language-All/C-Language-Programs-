#include<stdio.h>

//Use of fgetc and fputc in File IO

int main(){
/*
  //Let assume we have a text file which name is 'anku.text' and inside it "hello" written. If we want to read this file character by character so we can do this:-
  FILE *ptr;
  ptr = fopen("anku.txt","r");
  printf("%c",fgetc(ptr));
  printf("%c",fgetc(ptr));
  printf("%c",fgetc(ptr));
  printf("%c",fgetc(ptr));
  printf("%c",fgetc(ptr));
  
*/
  
  
  
  
  
  
  /*
  //Let assume we have a text file which name is 'anku.text' and inside it "hello" written. If we want to read this file character by character so we can do this:-
  FILE *ptr;
  ptr = fopen("anku.txt","r");
  printf("%c",getc(ptr));
  printf("%c",getc(ptr));
  printf("%c",getc(ptr));
  printf("%c",getc(ptr));
  printf("%c",getc(ptr));
  
*/





/*
 // Let assume we have a text file which name is 'anku.text'.If it's not exist then it will be creat automatically by itself.If we want to write "hello" character by character in this file so we can do this:-
  FILE *ptr;
  ptr = fopen("anku.txt","w");
  putc('h',ptr);
  putc('e',ptr);
  putc('l',ptr);
  putc('l',ptr);
  putc('o',ptr);
  fclose(ptr);
*/ 
  return 0;
}
