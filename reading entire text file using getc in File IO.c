#include <stdio.h>
int main(){
	
	
/*
//Let assume we have a text file "anku.txt" and we want to read entire text which is inside it. So we can use this:-
   FILE *ptr;
   ptr = fopen("anku.txt","r");
   char c = getc(ptr);
   while (c != EOF) {
      printf("%c", c);
     c = getc(ptr);
   }
   fclose(ptr);

*/
   
   
   
   
   
   
   
   
   
/*
//Let assume we have a text file 'anku.txt' ,which contain a string "hello saral".so we can read that :-
   FILE *ptr;
   char c;

   ptr = fopen("anku.txt", "r");

   while ((c = getc(ptr)) != EOF) {
      printf("%c", c);
   }
   fclose(ptr);
*/
	return 0;
	}
